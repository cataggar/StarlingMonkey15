package ts:typescript-system@0.1.0;

interface types {
    resource system {
        constructor();
        get-arguments: func() -> list<string>;
        hello: func() -> string;
    }
}

world root {
    import wasi:cli/environment@0.2.2;
    export types;
}