// using HostappWorld.wit.imports.example.component;
using HostappWorld.wit.imports.local.cowsay;

// uint left = 1;
// uint right = 2;
// uint result = AddInterop.Add(left, right);
// Console.WriteLine($"Hello {left} + {right} = {result}");

var tsVersion = CanvasInterop.TsVersion();
Console.WriteLine($"TS versino {tsVersion}");
